# Konfigurierbare Boards — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Die Info-Seite und das Athleten-Board durch ein einheitliches, konfigurierbares Board-System ersetzen (Spec: `docs/superpowers/specs/2026-08-10-boards-design.md`).

**Architecture:** Eine Tabelle `board` mit JSONB-Konfiguration; ein öffentlicher Resolve-Endpoint liefert Konfiguration + aufgelöste Timeline-Daten in einer Antwort; das Frontend rendert generisch (Grid → Kacheln → Elemente) und verwendet die gehärteten Athleten-Board-Karten wieder.

**Tech Stack:** Kotlin/Ktor/jOOQ/KIO (Backend), Flyway, React/MUI/TanStack Router (Frontend), hey-api-generierter Client aus handgepflegtem OpenAPI-YAML, Vitest, JUnit/Testcontainers.

## Global Constraints

- **Eigene Build-DB je Worktree** (geteilte auf 7652 driftet): `docker run -d --name r2r-boards-builddb -e POSTGRES_USER=developer -e POSTGRES_PASSWORD=sql -e POSTGRES_DB=ready2race-build -p 7662:5432 postgres:17` — **jeden** Maven-Aufruf mit `-Ddatabase.url=jdbc:postgresql://localhost:7662/ready2race-build` ausführen; nach DB-Wechsel einmal `clean`.
- `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home`; `mvnw` liegt in `backend/`.
- Migrationsnummer: **V202608102000** (nach V202608101120; Nummer am Ende dem Nutzer melden — Regel für parallele Sessions).
- OpenAPI ist handgepflegt: `backend/src/main/resources/openapi/documentation.yaml`; Client-Generierung: `cd frontend && npm run generate`.
- Keine Claude-Erwähnung in Commits. Deutsch mit echten Umlauten in UI-Texten und Kommentaren.
- Grenzwerte aus der Spec: Offset −6…+6, Listen-Limit 1–20, `refreshIntervalSeconds` ≥ 10 (Default 15), `rotationIntervalSeconds` ≥ 3 (Default 10), Layouts 1/2/3 Spalten + 6 Kacheln (3×2).
- i18n: alle drei Locales pflegen (`frontend/src/i18n/{de,en,da}/translations.json`).

---

### Task 1: Migration — Tabelle `board`, Default-Boards, Altlasten droppen

**Files:**
- Create: `backend/src/main/resources/db/migration/V202608102000__boards.sql`

**Interfaces:**
- Produces: Tabelle `board(id, event_id, name, config jsonb, created_at, updated_at)`; jOOQ-Typen `BOARD` / `BoardRecord` nach Codegen.

- [ ] **Step 1: Migration schreiben**

```sql
set search_path to ready2race, pg_catalog, public;

-- Boards ersetzen die Info-View-Konfiguration: ein Board je Bildschirm, die gesamte
-- Struktur (Layout, Kacheln, Elemente) als ein JSON-Dokument. Kein Fremdschlüssel zeigt
-- aus der Konfiguration heraus; geschrieben wird sie immer im Ganzen (siehe Design-Doku
-- docs/superpowers/specs/2026-08-10-boards-design.md).
create table board
(
    id         uuid primary key,
    event_id   uuid      not null references event (id) on delete cascade,
    name       text      not null,
    config     jsonb     not null,
    created_at timestamp not null,
    updated_at timestamp not null
);

create index idx_board_event_id on board (event_id);

-- Für jedes Event mit aktiver Athleten-Anzeige ein Default-Board, das die bisherige
-- Bühne reproduziert (drei Spalten: Lauf 0 / Lauf +1 / Lauf −1). showCountdown und der
-- Poll-Takt werden übernommen; montierte Bildschirme laufen über die alte URL weiter
-- (Frontend leitet auf das erste Board um).
insert into board (id, event_id, name, config, created_at, updated_at)
select gen_random_uuid(),
       ivc.event_id,
       'Athleten-Anzeige',
       jsonb_build_object(
           'layout', 'THREE_COLUMNS',
           'refreshIntervalSeconds', greatest(coalesce(ivc.display_duration_seconds, 15), 10),
           'tiles', jsonb_build_array(
               jsonb_build_object('rotationIntervalSeconds', 10, 'elements', jsonb_build_array(
                   jsonb_build_object('type', 'MATCH', 'offset', 0,
                                      'showCrew', true,
                                      'showCountdown', coalesce((ivc.filters ->> 'showCountdown')::boolean, true),
                                      'showTimes', true, 'contrastColors', true, 'autoFit', true))),
               jsonb_build_object('rotationIntervalSeconds', 10, 'elements', jsonb_build_array(
                   jsonb_build_object('type', 'MATCH', 'offset', 1,
                                      'showCrew', true,
                                      'showCountdown', coalesce((ivc.filters ->> 'showCountdown')::boolean, true),
                                      'showTimes', true, 'contrastColors', true, 'autoFit', true))),
               jsonb_build_object('rotationIntervalSeconds', 10, 'elements', jsonb_build_array(
                   jsonb_build_object('type', 'MATCH', 'offset', -1,
                                      'showCrew', true,
                                      'showCountdown', coalesce((ivc.filters ->> 'showCountdown')::boolean, true),
                                      'showTimes', true, 'contrastColors', true, 'autoFit', true)))
           )
       ),
       now(),
       now()
from (select distinct on (event_id) *
      from info_view_configuration
      where view_type = 'ATHLETE_BOARD'
        and is_active
      order by event_id, sort_order) ivc;

drop table info_view_configuration;
drop type info_view_type;
```

- [ ] **Step 2: Codegen laufen lassen und Kompilierfehler sichtbar machen**

Run: `cd backend && JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home ./mvnw clean compile -Ddatabase.url=jdbc:postgresql://localhost:7662/ready2race-build 2>&1 | tail -30`
Expected: FAIL — `InfoViewConfigurationRepo`, `Conversions`, `EventInfoService`, `DataEventExport` kompilieren nicht mehr (Tabelle weg). Das ist die Arbeitsliste für Tasks 2–5. `BOARD`-Tabelle ist generiert.

- [ ] **Step 3: Commit (nur Migration)**

```bash
git add backend/src/main/resources/db/migration/V202608102000__boards.sql
git commit -m "Add board table, seed default boards, drop info view configuration"
```

---

### Task 2: Konfigurations-Entities + Validierung (mit Tests)

**Files:**
- Create: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/entity/BoardConfig.kt`
- Create: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/entity/BoardDtos.kt`
- Test: `backend/src/test/kotlin/de/lambda9/ready2race/backend/app/eventInfo/BoardRequestValidationTest.kt`

**Interfaces:**
- Produces:
  - `enum class BoardLayout(val tileCount: Int, val columns: Int) { ONE_COLUMN(1,1), TWO_COLUMNS(2,2), THREE_COLUMNS(3,3), SIX_TILES(6,3) }`
  - `enum class BoardElementType { MATCH, MATCH_LIST, CLOCK, TEXT }`
  - `enum class BoardListMode { UPCOMING, RESULTS, RUNNING }`
  - `data class BoardElement(type, offset: Int?, showCrew/showCountdown/showTimes/contrastColors/autoFit: Boolean?, listMode: BoardListMode?, limit: Int?, showEventName: Boolean?, text: String?)` — flach statt sealed, damit OpenAPI/hey-api ohne Discriminator auskommt; Pflichtfelder je Typ prüft die Validierung.
  - `data class BoardTile(rotationIntervalSeconds: Int = 10, elements: List<BoardElement>)`
  - `data class BoardConfig(layout: BoardLayout, refreshIntervalSeconds: Int = 15, tiles: List<BoardTile>)`
  - `data class BoardDto(id: UUID, eventId: UUID, name: String, config: BoardConfig, createdAt: LocalDateTime, updatedAt: LocalDateTime)`
  - `data class BoardRequest(name: String, config: BoardConfig) : Validatable` mit `companion object { val example }`
  - `data class BoardNameDto(id: UUID, name: String)`

- [ ] **Step 1: Failing Test schreiben**

`BoardRequestValidationTest` — Konstanten aus `BoardConfig.kt` verwenden:

```kotlin
package de.lambda9.ready2race.backend.app.eventInfo

import de.lambda9.ready2race.backend.app.eventInfo.entity.*
import de.lambda9.ready2race.backend.validation.ValidationResult
import kotlin.test.Test
import kotlin.test.assertEquals
import kotlin.test.assertNotEquals

class BoardRequestValidationTest {

    private fun matchElement(offset: Int) = BoardElement(type = BoardElementType.MATCH, offset = offset)

    private fun request(config: BoardConfig) = BoardRequest(name = "Steg", config = config)

    private fun tiles(n: Int) = List(n) { BoardTile(elements = listOf(matchElement(0))) }

    @Test
    fun aMatchingTileCountIsValid() {
        val result = request(BoardConfig(layout = BoardLayout.THREE_COLUMNS, tiles = tiles(3))).validate()
        assertEquals(ValidationResult.Valid, result)
    }

    @Test
    fun aWrongTileCountIsInvalid() {
        val result = request(BoardConfig(layout = BoardLayout.SIX_TILES, tiles = tiles(3))).validate()
        assertNotEquals(ValidationResult.Valid, result)
    }

    @Test
    fun aTileWithoutElementsIsInvalid() {
        val config = BoardConfig(layout = BoardLayout.ONE_COLUMN, tiles = listOf(BoardTile(elements = emptyList())))
        assertNotEquals(ValidationResult.Valid, request(config).validate())
    }

    @Test
    fun aMatchElementNeedsAnOffsetInRange() {
        val missing = BoardConfig(
            layout = BoardLayout.ONE_COLUMN,
            tiles = listOf(BoardTile(elements = listOf(BoardElement(type = BoardElementType.MATCH)))),
        )
        assertNotEquals(ValidationResult.Valid, request(missing).validate())

        val outOfRange = BoardConfig(layout = BoardLayout.ONE_COLUMN, tiles = listOf(BoardTile(elements = listOf(matchElement(7)))))
        assertNotEquals(ValidationResult.Valid, request(outOfRange).validate())

        val edge = BoardConfig(layout = BoardLayout.ONE_COLUMN, tiles = listOf(BoardTile(elements = listOf(matchElement(-6)))))
        assertEquals(ValidationResult.Valid, request(edge).validate())
    }

    @Test
    fun aListElementNeedsModeAndLimitInRange() {
        fun listElement(mode: BoardListMode?, limit: Int?) = BoardConfig(
            layout = BoardLayout.ONE_COLUMN,
            tiles = listOf(BoardTile(elements = listOf(
                BoardElement(type = BoardElementType.MATCH_LIST, listMode = mode, limit = limit)
            ))),
        )
        assertEquals(ValidationResult.Valid, request(listElement(BoardListMode.UPCOMING, 5)).validate())
        assertNotEquals(ValidationResult.Valid, request(listElement(null, 5)).validate())
        assertNotEquals(ValidationResult.Valid, request(listElement(BoardListMode.UPCOMING, 0)).validate())
        assertNotEquals(ValidationResult.Valid, request(listElement(BoardListMode.UPCOMING, 21)).validate())
    }

    @Test
    fun aTextElementNeedsText() {
        val config = BoardConfig(
            layout = BoardLayout.ONE_COLUMN,
            tiles = listOf(BoardTile(elements = listOf(BoardElement(type = BoardElementType.TEXT, text = " ")))),
        )
        assertNotEquals(ValidationResult.Valid, request(config).validate())
    }

    @Test
    fun intervalsBelowTheFloorAreInvalid() {
        val fastRefresh = BoardConfig(layout = BoardLayout.ONE_COLUMN, refreshIntervalSeconds = 5, tiles = tiles(1))
        assertNotEquals(ValidationResult.Valid, request(fastRefresh).validate())

        val fastRotation = BoardConfig(
            layout = BoardLayout.ONE_COLUMN,
            tiles = listOf(BoardTile(rotationIntervalSeconds = 1, elements = listOf(matchElement(0)))),
        )
        assertNotEquals(ValidationResult.Valid, request(fastRotation).validate())

        val blankName = BoardRequest(name = " ", config = BoardConfig(layout = BoardLayout.ONE_COLUMN, tiles = tiles(1)))
        assertNotEquals(ValidationResult.Valid, blankName.validate())
    }
}
```

- [ ] **Step 2: Test laufen lassen — erwartet: kompiliert nicht** (Entities fehlen)

- [ ] **Step 3: Entities implementieren**

`BoardConfig.kt` (Konfiguration, die im JSONB liegt):

```kotlin
package de.lambda9.ready2race.backend.app.eventInfo.entity

/** Grenzen der Board-Konfiguration — eine Stelle für Backend-Validierung und Editor-Hinweise. */
object BoardLimits {
    const val MAX_OFFSET = 6
    const val MIN_LIST_LIMIT = 1
    const val MAX_LIST_LIMIT = 20
    const val MIN_REFRESH_INTERVAL_SECONDS = 10
    const val DEFAULT_REFRESH_INTERVAL_SECONDS = 15
    const val MIN_ROTATION_INTERVAL_SECONDS = 3
    const val DEFAULT_ROTATION_INTERVAL_SECONDS = 10
}

enum class BoardLayout(val tileCount: Int, val columns: Int) {
    ONE_COLUMN(1, 1),
    TWO_COLUMNS(2, 2),
    THREE_COLUMNS(3, 3),
    SIX_TILES(6, 3),
}

enum class BoardElementType { MATCH, MATCH_LIST, CLOCK, TEXT }

enum class BoardListMode { UPCOMING, RESULTS, RUNNING }

/**
 * Ein Element einer Kachel. Bewusst flach statt sealed: das Schema geht 1:1 durch das
 * handgepflegte OpenAPI-YAML und den hey-api-Generator, die mit einem Discriminator
 * beide mehr Reibung als Nutzen erzeugen. Welche Felder je [type] Pflicht sind,
 * erzwingt [BoardRequest.validate].
 */
data class BoardElement(
    val type: BoardElementType,
    // MATCH: Position auf der Tages-Timeline, 0 = zuletzt gestarteter noch laufender Lauf.
    val offset: Int? = null,
    val showCrew: Boolean? = null,
    val showCountdown: Boolean? = null,
    val showTimes: Boolean? = null,
    val contrastColors: Boolean? = null,
    val autoFit: Boolean? = null,
    // MATCH_LIST
    val listMode: BoardListMode? = null,
    val limit: Int? = null,
    // CLOCK
    val showEventName: Boolean? = null,
    // TEXT
    val text: String? = null,
)

data class BoardTile(
    val rotationIntervalSeconds: Int = BoardLimits.DEFAULT_ROTATION_INTERVAL_SECONDS,
    val elements: List<BoardElement>,
)

data class BoardConfig(
    val layout: BoardLayout,
    val refreshIntervalSeconds: Int = BoardLimits.DEFAULT_REFRESH_INTERVAL_SECONDS,
    val tiles: List<BoardTile>,
)
```

`BoardDtos.kt` (CRUD-DTOs; die View-DTOs kommen in Task 3):

```kotlin
package de.lambda9.ready2race.backend.app.eventInfo.entity

import de.lambda9.ready2race.backend.validation.Validatable
import de.lambda9.ready2race.backend.validation.ValidationResult
import de.lambda9.ready2race.backend.validation.validate
import de.lambda9.ready2race.backend.validation.validators.StringValidators.notBlank
import java.time.LocalDateTime
import java.util.UUID

data class BoardDto(
    val id: UUID,
    val eventId: UUID,
    val name: String,
    val config: BoardConfig,
    val createdAt: LocalDateTime,
    val updatedAt: LocalDateTime,
)

data class BoardNameDto(
    val id: UUID,
    val name: String,
)

data class BoardRequest(
    val name: String,
    val config: BoardConfig,
) : Validatable {

    override fun validate(): ValidationResult = ValidationResult.allOf(
        this::name validate notBlank,
        configResult(),
    )

    // Die Struktur ist zu verschachtelt für die Feld-DSL; ein sprechender Fehlertext je
    // Regel reicht der Admin-Maske.
    private fun configResult(): ValidationResult {
        val errors = mutableListOf<String>()
        if (config.tiles.size != config.layout.tileCount) {
            errors += "layout ${config.layout} expects ${config.layout.tileCount} tiles, got ${config.tiles.size}"
        }
        if (config.refreshIntervalSeconds < BoardLimits.MIN_REFRESH_INTERVAL_SECONDS) {
            errors += "refreshIntervalSeconds must be at least ${BoardLimits.MIN_REFRESH_INTERVAL_SECONDS}"
        }
        config.tiles.forEachIndexed { tileIndex, tile ->
            if (tile.elements.isEmpty()) errors += "tile $tileIndex has no elements"
            if (tile.rotationIntervalSeconds < BoardLimits.MIN_ROTATION_INTERVAL_SECONDS) {
                errors += "tile $tileIndex: rotationIntervalSeconds must be at least ${BoardLimits.MIN_ROTATION_INTERVAL_SECONDS}"
            }
            tile.elements.forEachIndexed { elementIndex, element ->
                val at = "tile $tileIndex element $elementIndex"
                when (element.type) {
                    BoardElementType.MATCH -> {
                        val offset = element.offset
                        if (offset == null || offset < -BoardLimits.MAX_OFFSET || offset > BoardLimits.MAX_OFFSET) {
                            errors += "$at: MATCH needs offset in -${BoardLimits.MAX_OFFSET}..${BoardLimits.MAX_OFFSET}"
                        }
                    }
                    BoardElementType.MATCH_LIST -> {
                        if (element.listMode == null) errors += "$at: MATCH_LIST needs listMode"
                        val limit = element.limit
                        if (limit == null || limit < BoardLimits.MIN_LIST_LIMIT || limit > BoardLimits.MAX_LIST_LIMIT) {
                            errors += "$at: MATCH_LIST needs limit in ${BoardLimits.MIN_LIST_LIMIT}..${BoardLimits.MAX_LIST_LIMIT}"
                        }
                    }
                    BoardElementType.TEXT -> {
                        if (element.text.isNullOrBlank()) errors += "$at: TEXT needs text"
                    }
                    BoardElementType.CLOCK -> {}
                }
            }
        }
        return if (errors.isEmpty()) ValidationResult.Valid
        else ValidationResult.Invalid.Message { errors.joinToString("; ") }
    }

    companion object {
        val example = BoardRequest(
            name = "Athleten-Anzeige",
            config = BoardConfig(
                layout = BoardLayout.THREE_COLUMNS,
                tiles = listOf(
                    BoardTile(elements = listOf(BoardElement(type = BoardElementType.MATCH, offset = 0))),
                    BoardTile(elements = listOf(BoardElement(type = BoardElementType.MATCH, offset = 1))),
                    BoardTile(elements = listOf(BoardElement(type = BoardElementType.MATCH, offset = -1))),
                ),
            ),
        )
    }
}
```

- [ ] **Step 4: Test laufen lassen**

Run: `cd backend && JAVA_HOME=… ./mvnw test -Dtest=BoardRequestValidationTest -Ddatabase.url=jdbc:postgresql://localhost:7662/ready2race-build 2>&1 | tail -15`
Expected: kompiliert noch nicht ganz grün, weil Task-1-Bruchstellen (`InfoViewConfigurationRepo` etc.) das Modul blockieren — falls so, Tasks 4–5 ziehen die Bruchstellen; alternativ Test nach Task 5 erneut. Wenn kompilierbar: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/entity/BoardConfig.kt \
        backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/entity/BoardDtos.kt \
        backend/src/test/kotlin/de/lambda9/ready2race/backend/app/eventInfo/BoardRequestValidationTest.kt
git commit -m "Add board config entities with validation"
```

---

### Task 3: BoardLogic — Timeline-Auflösung und Datenbedarf (mit Tests)

**Files:**
- Create: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/boundary/BoardLogic.kt`
- Create (View-DTOs): `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/entity/BoardViewDto.kt`
- Test: `backend/src/test/kotlin/de/lambda9/ready2race/backend/app/eventInfo/BoardLogicTest.kt`

**Interfaces:**
- Consumes: `AthleteBoardMatch`, `AthleteBoardResult` (bestehende Entities), `BoardConfig`/`BoardElement` (Task 2).
- Produces:
  - `data class BoardMatchSlotDto(offset: Int, match: AthleteBoardMatch?, result: AthleteBoardResult?)`
  - `data class BoardListDto(mode: BoardListMode, matches: List<AthleteBoardMatch>, results: List<AthleteBoardResult>)`
  - `data class BoardViewDto(boardId: UUID, eventName: String, serverTime: LocalDateTime, refreshIntervalSeconds: Int, config: BoardConfig, slots: List<BoardMatchSlotDto>, lists: List<BoardListDto>)`
  - `BoardLogic.dataNeeds(config: BoardConfig): BoardDataNeeds` mit `data class BoardDataNeeds(runningLimit: Int, upcomingLimit: Int, resultsLimit: Int, offsets: Set<Int>, listLimits: Map<BoardListMode, Int>)`
  - `BoardLogic.resolveOffset(offset: Int, running: List<AthleteBoardMatch>, upcoming: List<AthleteBoardMatch>, results: List<AthleteBoardResult>): BoardMatchSlotDto`

- [ ] **Step 1: Failing Tests schreiben** — Kernfälle:

```kotlin
package de.lambda9.ready2race.backend.app.eventInfo

import de.lambda9.ready2race.backend.app.eventInfo.boundary.BoardLogic
import de.lambda9.ready2race.backend.app.eventInfo.entity.*
import de.lambda9.ready2race.backend.app.matchStatus.entity.MatchState
import java.time.LocalDateTime
import java.util.UUID
import kotlin.test.Test
import kotlin.test.assertEquals
import kotlin.test.assertNull

class BoardLogicTest {

    private fun match(name: String) = AthleteBoardMatch(
        matchId = UUID.randomUUID(), competitionName = name, categoryName = null,
        roundName = null, matchName = null, startTime = null,
        state = MatchState.RUNNING, startState = AthleteBoardStartState.UNSCHEDULED,
        teams = emptyList(),
    )

    private fun result(name: String) = AthleteBoardResult(
        matchId = UUID.randomUUID(), competitionName = name, categoryName = null,
        roundName = null, matchName = null, startTime = null, actualStartTime = null,
        teams = emptyList(),
    )

    // running aufsteigend nach tatsächlichem Start, results neuestes zuerst, upcoming aufsteigend.
    private val running = listOf(match("R-früh"), match("R-spät"))
    private val upcoming = listOf(match("U1"), match("U2"))
    private val results = listOf(result("E-neu"), result("E-alt"))

    @Test
    fun zeroIsTheLastStartedRunningMatch() {
        assertEquals("R-spät", BoardLogic.resolveOffset(0, running, upcoming, results).match?.competitionName)
    }

    @Test
    fun negativeOffsetsWalkThroughEarlierRunningThenResults() {
        // -1 ist der parallel laufende frühere Lauf, erst -2 erreicht die Ergebnisse.
        assertEquals("R-früh", BoardLogic.resolveOffset(-1, running, upcoming, results).match?.competitionName)
        assertEquals("E-neu", BoardLogic.resolveOffset(-2, running, upcoming, results).result?.competitionName)
        assertEquals("E-alt", BoardLogic.resolveOffset(-3, running, upcoming, results).result?.competitionName)
    }

    @Test
    fun withoutRunningZeroIsEmptyAndNeighboursExist() {
        val slot0 = BoardLogic.resolveOffset(0, emptyList(), upcoming, results)
        assertNull(slot0.match); assertNull(slot0.result)
        assertEquals("E-neu", BoardLogic.resolveOffset(-1, emptyList(), upcoming, results).result?.competitionName)
        assertEquals("U1", BoardLogic.resolveOffset(1, emptyList(), upcoming, results).match?.competitionName)
    }

    @Test
    fun positiveOffsetsIndexUpcoming() {
        assertEquals("U2", BoardLogic.resolveOffset(2, running, upcoming, results).match?.competitionName)
        val beyond = BoardLogic.resolveOffset(3, running, upcoming, results)
        assertNull(beyond.match); assertNull(beyond.result)
    }

    @Test
    fun dataNeedsCoverOffsetsAndLists() {
        val config = BoardConfig(
            layout = BoardLayout.TWO_COLUMNS,
            tiles = listOf(
                BoardTile(elements = listOf(
                    BoardElement(type = BoardElementType.MATCH, offset = -4),
                    BoardElement(type = BoardElementType.MATCH, offset = 2),
                )),
                BoardTile(elements = listOf(
                    BoardElement(type = BoardElementType.MATCH_LIST, listMode = BoardListMode.UPCOMING, limit = 8),
                    BoardElement(type = BoardElementType.MATCH_LIST, listMode = BoardListMode.UPCOMING, limit = 3),
                    BoardElement(type = BoardElementType.TEXT, text = "Hi"),
                )),
            ),
        )
        val needs = BoardLogic.dataNeeds(config)
        assertEquals(setOf(-4, 2), needs.offsets)
        // Negative Offsets können in parallel laufende Läufe zeigen: |min|+1 laufende abrufen.
        assertEquals(5, needs.runningLimit)
        assertEquals(8, needs.upcomingLimit)   // max(Offset +2, Liste 8)
        assertEquals(4, needs.resultsLimit)    // |−4|
        assertEquals(mapOf(BoardListMode.UPCOMING to 8), needs.listLimits)
    }

    @Test
    fun dataNeedsWithoutMatchElementsStayMinimal() {
        val config = BoardConfig(
            layout = BoardLayout.ONE_COLUMN,
            tiles = listOf(BoardTile(elements = listOf(BoardElement(type = BoardElementType.CLOCK)))),
        )
        val needs = BoardLogic.dataNeeds(config)
        assertEquals(emptySet<Int>(), needs.offsets)
        assertEquals(1, needs.runningLimit)
        assertEquals(1, needs.upcomingLimit)
        assertEquals(1, needs.resultsLimit)
    }
}
```

- [ ] **Step 2: Test laufen lassen — FAIL** (BoardLogic fehlt)

- [ ] **Step 3: Implementieren**

`BoardViewDto.kt`:

```kotlin
package de.lambda9.ready2race.backend.app.eventInfo.entity

import java.time.LocalDateTime
import java.util.UUID

/**
 * Ein Platz auf der Tages-Timeline. Höchstens eines von [match] und [result] ist gefüllt;
 * beide leer heißt: an dieser Position ist (noch) nichts — die Kachel zeigt ihren
 * Leerzustand, statt zu verschwinden.
 */
data class BoardMatchSlotDto(
    val offset: Int,
    val match: AthleteBoardMatch?,
    val result: AthleteBoardResult?,
)

/** Daten eines Listen-Elements; je [mode] ist genau eine der beiden Listen gefüllt. */
data class BoardListDto(
    val mode: BoardListMode,
    val matches: List<AthleteBoardMatch>,
    val results: List<AthleteBoardResult>,
)

/** Alles, was die Anzeige eines Boards braucht, in einer Antwort. */
data class BoardViewDto(
    val boardId: UUID,
    val eventName: String,
    val serverTime: LocalDateTime,
    val refreshIntervalSeconds: Int,
    val config: BoardConfig,
    val slots: List<BoardMatchSlotDto>,
    val lists: List<BoardListDto>,
)
```

`BoardLogic.kt`:

```kotlin
package de.lambda9.ready2race.backend.app.eventInfo.boundary

import de.lambda9.ready2race.backend.app.eventInfo.entity.*

/**
 * Reine Logik der Boards: Timeline-Auflösung und Datenbedarf, ohne Datenbank- und
 * Ktor-Bezug — prüfbar ohne laufende Umgebung, analog [AthleteBoardLogic].
 *
 * Die Timeline eines Tages: beendete Läufe (chronologisch) → laufende (nach
 * tatsächlichem Start) → anstehende (nach geplantem Start). Der Cursor 0 steht auf dem
 * zuletzt gestarteten, noch laufenden Lauf; läuft nichts, steht er zwischen letztem
 * Ergebnis und nächstem Start und 0 ist leer. Die Darstellung folgt dem Zustand des
 * Laufs, nicht dem Vorzeichen des Offsets.
 */
object BoardLogic {

    const val CACHE_TTL_SECONDS = AthleteBoardLogic.CACHE_TTL_SECONDS

    data class BoardDataNeeds(
        val runningLimit: Int,
        val upcomingLimit: Int,
        val resultsLimit: Int,
        val offsets: Set<Int>,
        val listLimits: Map<BoardListMode, Int>,
    )

    /**
     * Welche Datenmengen ein Board braucht. Listen desselben Modus werden auf ihr
     * größtes Limit zusammengelegt; die Anzeige schneidet je Element selbst zu.
     */
    fun dataNeeds(config: BoardConfig): BoardDataNeeds {
        val elements = config.tiles.flatMap { it.elements }
        val offsets = elements
            .filter { it.type == BoardElementType.MATCH }
            .mapNotNull { it.offset }
            .toSet()
        val listLimits = elements
            .filter { it.type == BoardElementType.MATCH_LIST && it.listMode != null }
            .groupBy { it.listMode!! }
            .mapValues { (_, es) -> es.maxOf { it.limit ?: BoardLimits.MIN_LIST_LIMIT } }

        val maxNegative = offsets.filter { it < 0 }.minOrNull()?.let { -it } ?: 0
        val maxPositive = offsets.filter { it > 0 }.maxOrNull() ?: 0

        // Negative Offsets können parallel laufende Läufe treffen, bevor sie die
        // Ergebnisse erreichen — deshalb |min|+1 laufende Läufe abrufen, nie unter 1,
        // damit "läuft gerade nichts" von "nichts abgefragt" unterscheidbar bleibt.
        return BoardDataNeeds(
            runningLimit = maxOf(1, maxNegative + 1, listLimits[BoardListMode.RUNNING] ?: 0),
            upcomingLimit = maxOf(1, maxPositive, listLimits[BoardListMode.UPCOMING] ?: 0),
            resultsLimit = maxOf(1, maxNegative, listLimits[BoardListMode.RESULTS] ?: 0),
            offsets = offsets,
            listLimits = listLimits,
        )
    }

    /**
     * Löst einen Offset gegen die drei Blöcke auf. [running] aufsteigend nach
     * tatsächlichem Start, [upcoming] aufsteigend nach geplantem Start, [results]
     * neuestes zuerst — genau die Reihenfolgen, die die bestehenden Abfragen liefern.
     */
    fun resolveOffset(
        offset: Int,
        running: List<AthleteBoardMatch>,
        upcoming: List<AthleteBoardMatch>,
        results: List<AthleteBoardResult>,
    ): BoardMatchSlotDto = when {
        offset > 0 -> BoardMatchSlotDto(offset, upcoming.getOrNull(offset - 1), null)
        offset == 0 -> BoardMatchSlotDto(offset, running.lastOrNull(), null)
        else -> {
            // Rückwärts vom Cursor: erst die früher gestarteten, noch laufenden Läufe,
            // dann die Ergebnisse (neuestes zuerst).
            val earlierRunning = if (running.isEmpty()) emptyList() else running.dropLast(1).reversed()
            val steps = -offset
            if (steps <= earlierRunning.size) {
                BoardMatchSlotDto(offset, earlierRunning[steps - 1], null)
            } else {
                BoardMatchSlotDto(offset, null, results.getOrNull(steps - earlierRunning.size - 1))
            }
        }
    }
}
```

- [ ] **Step 4: Test laufen lassen — PASS** (`-Dtest=BoardLogicTest`)

- [ ] **Step 5: Commit**

```bash
git add backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/boundary/BoardLogic.kt \
        backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/entity/BoardViewDto.kt \
        backend/src/test/kotlin/de/lambda9/ready2race/backend/app/eventInfo/BoardLogicTest.kt
git commit -m "Add board timeline logic with offset resolution"
```

---

### Task 4: BoardRepo + Conversions

**Files:**
- Create: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/control/BoardRepo.kt`
- Modify: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/control/Conversions.kt` (InfoView-Konvertierungen raus, Board-Konvertierungen rein)

**Interfaces:**
- Consumes: generierte `BOARD`-Tabelle/`BoardRecord` (Task 1), `objectMapper` wie in `EventInfoService` (Import prüfen: gleiche Quelle wie dort für `filters`-Parsing, `de.lambda9.ready2race.backend.serialization.jsonMapper` o. ä. — beim Umsetzen nachschlagen).
- Produces:
  - `BoardRepo.findByEvent(eventId): JIO<List<BoardRecord>>` (sortiert nach `CREATED_AT.asc()` — stabile Reihenfolge, „erstes Board" für die Umleitung)
  - `BoardRepo.findById(id)`, `create(record)`, `update(id, f)`, `delete(id)`, `getAsJson(eventId)`, `insertJsonData(data)`
  - `BoardRecord.toDto(): BoardDto` (Config aus JSONB parsen), `BoardRequest.toRecord(eventId: UUID): BoardRecord` (Config serialisieren, `id = UUID.randomUUID()`, Zeitstempel `LocalDateTime.now()`)

- [ ] **Step 1: BoardRepo nach dem Muster von `InfoViewConfigurationRepo` schreiben** (`selectOne`/`insertReturning`/`update`/`delete`/`exists`/`selectAsJson`/`insertJsonData` aus `de.lambda9.ready2race.backend.database.*`).

- [ ] **Step 2: In `Conversions.kt`** `InfoViewConfigurationRecord.toDto`/`InfoViewConfigurationRequest.toRecord` entfernen und ersetzen durch:

```kotlin
fun BoardRecord.toDto(): BoardDto = BoardDto(
    id = id!!,
    eventId = eventId!!,
    name = name!!,
    config = jsonMapper.readValue(config!!.data(), BoardConfig::class.java),
    createdAt = createdAt!!,
    updatedAt = updatedAt!!,
)

fun BoardRequest.toRecord(eventId: UUID): BoardRecord {
    val now = LocalDateTime.now()
    return BoardRecord(
        id = UUID.randomUUID(),
        eventId = eventId,
        name = name,
        config = JSONB.jsonb(jsonMapper.writeValueAsString(config)),
        createdAt = now,
        updatedAt = now,
    )
}
```

(Exakte Nullbarkeit/Feldnamen an den generierten `BoardRecord` anpassen; `JSONB` aus `org.jooq`. Welcher ObjectMapper: dieselbe Instanz, die `EventInfoService` heute für `filters` nutzt — nachschlagen und wiederverwenden, nicht neu bauen.)

- [ ] **Step 3: Commit** (`git commit -m "Add board repo and conversions"`)

---

### Task 5: BoardService + Routen + Aufräumen von InfoViews/AthleteBoard + webDAV

**Files:**
- Create: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/boundary/BoardService.kt`
- Modify: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/boundary/EventInfoService.kt`
- Modify: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/boundary/eventInfo.kt`
- Modify: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/webDAV/entity/DataEventExport.kt`
- Delete: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/control/InfoViewConfigurationRepo.kt`, `entity/InfoViewConfigurationDto.kt`, `entity/InfoViewConfigurationRequest.kt`, `entity/AthleteBoardConfig.kt`, `entity/AthleteBoardDto.kt` **nur** die Klasse `AthleteBoardDto` (die übrigen Klassen der Datei bleiben)
- Modify: `backend/src/main/kotlin/de/lambda9/ready2race/backend/app/eventInfo/boundary/AthleteBoardLogic.kt` (`resolveConfig` + zugehörige Konstanten/`limitOr` entfernen; `isPublicResult`, `startState`, `isStillUpcoming`, `isCacheFresh`, `sortByStartTime`, Platzhalter-Funktionen bleiben)

**Interfaces:**
- Consumes: `BoardRepo`, `BoardLogic`, `EventInfoService`-Helfer.
- Produces (BoardService):
  - `getBoards(eventId): App<EventInfoProblem, ApiResponse.ListDto<BoardDto>>`
  - `getBoardNames(eventId): App<EventInfoProblem, ApiResponse.ListDto<BoardNameDto>>` (öffentlich)
  - `createBoard(eventId, request): App<…, ApiResponse.Dto<BoardDto>>`
  - `updateBoard(boardId, request)`, `deleteBoard(boardId)` (404 → `EventInfoProblem.NotFound`, vorhandenes Muster in `EventInfoProblem` prüfen und nachziehen)
  - `getBoardView(eventId, boardId): App<EventInfoProblem, ApiResponse.Dto<BoardViewDto>>` — Cache je `boardId` (TTL `BoardLogic.CACHE_TTL_SECONDS`, `serverTime` je Antwort frisch, Invalidierung in `updateBoard`/`deleteBoard`)

- [ ] **Step 1: EventInfoService-Helfer öffnen** — die drei privaten Overloads `getRunningMatches(eventId, limit, clubShortNames)`, `getUpcomingMatchesForBoard(eventId, limit, clubShortNames)`, `getLatestMatchResults(eventId, limit, competitionId, clubShortNames)` sowie `clubShortNames()` auf `internal` setzen. Entfernen: `getInfoViews`, `createInfoView`, `updateInfoView`, `deleteInfoView`, `getAthleteBoard`, `athleteBoardCache`, `CachedBoard` und die zugehörigen Importe.

- [ ] **Step 2: BoardService implementieren.** Kern von `getBoardView` (Aufbau wie das alte `getAthleteBoard`):

```kotlin
private data class CachedView(val builtAt: LocalDateTime, val dto: BoardViewDto)
private val boardViewCache = ConcurrentHashMap<UUID, CachedView>()

fun getBoardView(eventId: UUID, boardId: UUID): App<EventInfoProblem, ApiResponse.Dto<BoardViewDto>> =
    KIO.comprehension {
        val now = LocalDateTime.now()
        val cached = boardViewCache[boardId]?.takeIf { AthleteBoardLogic.isCacheFresh(it.builtAt, now) }
        if (cached != null) {
            KIO.ok(ApiResponse.Dto(cached.dto.copy(serverTime = now)))
        } else {
            val record = !BoardRepo.findById(boardId).orDie()
            if (record == null || record.eventId != eventId) {
                !KIO.fail<EventInfoProblem>(EventInfoProblem.BoardNotFound(boardId))
            }
            val eventName = !EventRepo.getName(eventId).orDie()
                ?: !KIO.fail<EventInfoProblem>(EventInfoProblem.EventNotFound(eventId))
            val board = record!!.toDto()
            val needs = BoardLogic.dataNeeds(board.config)
            val clubShortNames = !clubShortNames()

            // startState immer mit Countdown berechnen; ob der Countdown erscheint,
            // entscheidet das Element auf dem Gerät (AthleteBoardMatchCard).
            val running = (!EventInfoService.getRunningMatches(eventId, needs.runningLimit, clubShortNames))
                .data.map { it.toAthleteBoardMatch(now, showCountdown = true) }
            val upcoming = AthleteBoardLogic.sortByStartTime(
                (!EventInfoService.getUpcomingMatchesForBoard(eventId, needs.upcomingLimit, clubShortNames))
                    .data.map { it.toAthleteBoardMatch(now, showCountdown = true) }
            ) { it.startTime }
            val results = (!EventInfoService.getLatestMatchResults(eventId, needs.resultsLimit, null, clubShortNames))
                .data.map { it.toAthleteBoardResult() }

            val dto = BoardViewDto(
                boardId = board.id,
                eventName = eventName,
                serverTime = now,
                refreshIntervalSeconds = board.config.refreshIntervalSeconds
                    .coerceAtLeast(BoardLimits.MIN_REFRESH_INTERVAL_SECONDS),
                config = board.config,
                slots = needs.offsets.sorted().map { BoardLogic.resolveOffset(it, running, upcoming, results) },
                lists = needs.listLimits.map { (mode, limit) ->
                    when (mode) {
                        BoardListMode.RUNNING -> BoardListDto(mode, running.take(limit), emptyList())
                        BoardListMode.UPCOMING -> BoardListDto(mode, upcoming.take(limit), emptyList())
                        BoardListMode.RESULTS -> BoardListDto(mode, emptyList(), results.take(limit))
                    }
                },
            )
            boardViewCache[boardId] = CachedView(now, dto)
            KIO.ok(ApiResponse.Dto(dto))
        }
    }
```

Achtung Reihenfolge-Annahme: `getLatestMatchResults` muss **neuestes zuerst** liefern und `getRunningMatches` **aufsteigend nach Start** — beim Umsetzen an den Repos verifizieren (`CompetitionMatchRepo.getRunningMatches` / `getMatchResults` Order-by lesen) und andernfalls hier sortieren, mit Kommentar.

CRUD-Methoden: `getBoards` = `findByEvent` + `map { it.toDto() }`; `createBoard` = `toRecord` + `create` + Rückgabe `toDto`; `updateBoard` = `update { name = …; config = …; updatedAt = now }` + Cache-Invalidierung + 404-Fall; `deleteBoard` analog (`delete` liefert Zeilenzahl; 0 → NotFound). Neues Problem-Member `EventInfoProblem.BoardNotFound(boardId)` nach Muster von `EventNotFound` inkl. `respond`-Zweig.

- [ ] **Step 3: Routen in `eventInfo.kt`.** Im öffentlichen `rateLimit`-Block: `get("/athlete-board")` **entfernen**, stattdessen:

```kotlin
// Boards sind öffentlich abrufbar wie die Anzeigen darüber: montierte Bildschirme
// und Athleten-Handys laden ihre URL ohne Anmeldung.
get("/boards") {
    call.respondComprehension {
        val eventId = !pathParam("eventId", uuid)
        BoardService.getBoardNames(eventId)
    }
}
get("/board/{boardId}") {
    call.respondComprehension {
        val eventId = !pathParam("eventId", uuid)
        val boardId = !pathParam("boardId", uuid)
        BoardService.getBoardView(eventId, boardId)
    }
}
```

Den kompletten `route("/event/{eventId}/info-views")`-Block ersetzen durch `route("/event/{eventId}/boards")` mit denselben Privilegien: GET (Read) → `getBoards`, POST (Update) → `createBoard` mit `receiveKIO(BoardRequest.example)`, PUT `/{boardId}` → `updateBoard`, DELETE `/{boardId}` → `deleteBoard`.

- [ ] **Step 4: webDAV umstellen.** In `DataEventExport`: Feld `infoViewConfigurations` → `val boards: JsonNode?` (nullable: ältere Exportdateien haben das Feld nicht; deren Info-View-Daten sind mit dem Konzept obsolet und werden nicht importiert). Export: `BoardRepo.getAsJson(eventId)`, Map-Key `"boards"`. Import: `data.boards?.let { !BoardRepo.insertJsonData(it.toString()).orDie() }`. Prüfen, dass der Deserialisierer unbekannte Felder ignoriert (Serialization.kt: `FAIL_ON_UNKNOWN_PROPERTIES` aus?) — sonst `@JsonIgnoreProperties(ignoreUnknown = true)` an die Klasse.

- [ ] **Step 5: Kompilieren + alle Backend-Tests**

Run: `cd backend && JAVA_HOME=… ./mvnw clean test -Ddatabase.url=jdbc:postgresql://localhost:7662/ready2race-build 2>&1 | tail -25`
Expected: BUILD SUCCESS; `BoardRequestValidationTest`/`BoardLogicTest` grün; keine Restverweise auf InfoView*.

- [ ] **Step 6: Commit** (`git commit -m "Replace info view endpoints with board service"`)

---

### Task 6: DB-Test für BoardService (Testcontainers)

**Files:**
- Test: `backend/src/test/kotlin/de/lambda9/ready2race/backend/app/eventInfo/BoardServiceTest.kt`

**Interfaces:**
- Consumes: `testComprehension` (Muster: `ClubShortNameRepoTest`), `BoardService`, `BoardRepo`.

- [ ] **Step 1: Test schreiben** — Umlauf über die Service-Schicht gegen echtes Postgres:

```kotlin
package de.lambda9.ready2race.backend.app.eventInfo

import de.lambda9.ready2race.backend.app.eventInfo.boundary.BoardService
import de.lambda9.ready2race.backend.app.eventInfo.entity.*
import de.lambda9.ready2race.testing.testComprehension
import kotlin.test.*

class BoardServiceTest {

    // Event anlegen: vorhandene Test-Fixtures im Testbaum suchen (z. B. wie andere
    // Service-Tests ein EventRecord einfügen) und hier wiederverwenden.

    @Test
    fun boardRoundTripAndViewResolution() = testComprehension {
        val eventId = /* Fixture: Event einfügen, ID zurück */

        val created = (!BoardService.createBoard(eventId, BoardRequest.example)).dto

        val listed = (!BoardService.getBoards(eventId)).dtos
        assertEquals(listOf(created.id), listed.map { it.id })

        // View: leeres Event → Slots vorhanden, aber leer; Struktur bleibt stehen.
        val view = (!BoardService.getBoardView(eventId, created.id)).dto
        assertEquals(setOf(0, 1, -1), view.slots.map { it.offset }.toSet())
        assertTrue(view.slots.all { it.match == null && it.result == null })
        assertEquals(BoardLayout.THREE_COLUMNS, view.config.layout)

        // Update ändert Namen und invalidiert den Cache.
        !BoardService.updateBoard(created.id, BoardRequest.example.copy(name = "Zielturm"))
        assertEquals("Zielturm", (!BoardService.getBoards(eventId)).dtos.single().name)

        !BoardService.deleteBoard(created.id)
        assertTrue((!BoardService.getBoards(eventId)).dtos.isEmpty())
    }
}
```

(`ApiResponse.Dto`/`ListDto`-Zugriffsformen an die tatsächlichen Klassen anpassen; Event-Fixture aus bestehenden Tests übernehmen — vor dem Schreiben `grep -rn "EventRepo.create\|EventRecord(" backend/src/test` und das dortige Muster kopieren.)

- [ ] **Step 2: Test laufen lassen — PASS** (mit `-Ddatabase.url=…7662…`)

- [ ] **Step 3: Commit** (`git commit -m "Add board service db test"`)

---

### Task 7: OpenAPI-Spezifikation aktualisieren + Client generieren

**Files:**
- Modify: `backend/src/main/resources/openapi/documentation.yaml`
- Generated: `frontend/src/api/*` via `npm run generate`

**Interfaces:**
- Produces (Frontend-SDK): `getBoards`, `createBoard`, `updateBoard`, `deleteBoard`, `getPublicBoards`, `getBoardView`; Typen `BoardDto`, `BoardRequest`, `BoardConfig`, `BoardTile`, `BoardElement`, `BoardLayout`, `BoardElementType`, `BoardListMode`, `BoardNameDto`, `BoardViewDto`, `BoardMatchSlotDto`, `BoardListDto`.

- [ ] **Step 1: YAML anpassen.**
  - Entfernen: Pfade `/event/{eventId}/info/athlete-board`, `/event/{eventId}/info-views`, `/event/{eventId}/info-views/{viewId}`; Schemas `InfoViewType`, `InfoViewConfigurationDto`, `InfoViewConfigurationRequest`, `AthleteBoardDto` (die AthleteBoard-Unterschemas `AthleteBoardMatch`, `AthleteBoardResult`, … bleiben).
  - Hinzufügen (operationIds exakt so, sie werden zu SDK-Funktionsnamen):
    - `GET /event/{eventId}/boards` → `getBoards` (200: array of BoardDto), `POST` → `createBoard` (Body BoardRequest, 200 BoardDto)
    - `PUT /event/{eventId}/boards/{boardId}` → `updateBoard`, `DELETE` → `deleteBoard`
    - `GET /event/{eventId}/info/boards` → `getPublicBoards` (200: array of BoardNameDto)
    - `GET /event/{eventId}/info/board/{boardId}` → `getBoardView` (200: BoardViewDto, 404)
  - Schemas nach dem Muster der Nachbarn; `BoardElement` mit `type` (enum) required, alle übrigen Felder optional; `BoardConfig.layout` enum ONE_COLUMN/TWO_COLUMNS/THREE_COLUMNS/SIX_TILES; `BoardViewDto.slots`/`lists` als Arrays.

- [ ] **Step 2: Client generieren**

Run: `cd frontend && npm run generate && npx tsc --noEmit 2>&1 | head -30`
Expected: Generierung ok; tsc zeigt die erwarteten Bruchstellen in den alten Komponenten (`InfoViewConfigurationDto` weg) — Arbeitsliste für Tasks 8–11.

- [ ] **Step 3: Commit** (`git commit -m "Update OpenAPI spec and generated client for boards"`)

---

### Task 8: Frontend-Anzeige — Slot-Logik + Board-Renderer (mit Tests)

**Files:**
- Create: `frontend/src/components/event/board/boardView.ts` (pure Logik)
- Create: `frontend/src/components/event/board/boardView.test.ts`
- Create: `frontend/src/components/event/board/useBoardViewData.ts`
- Create: `frontend/src/components/event/board/BoardRenderer.tsx`
- Create: `frontend/src/components/event/board/BoardTileView.tsx`
- Create: `frontend/src/components/event/board/BoardElementView.tsx`
- Create: `frontend/src/components/event/board/BoardMatchSlotElement.tsx`
- Create: `frontend/src/components/event/board/BoardMatchListElement.tsx`
- Create: `frontend/src/components/event/board/BoardClockElement.tsx`
- Create: `frontend/src/components/event/board/BoardTextElement.tsx`

**Interfaces:**
- Consumes: generierte Typen (Task 7), `AthleteBoardMatchCard`, `AthleteBoardResultCard`, `AthleteBoardColumnCard`, `useServerClock`, `densityScale`/`rowTextLines`/`longestTeamLabel`/`maxBoats` aus `athleteBoard/boardLayout.ts`, `usePolledEndpoint`.
- Produces:
  - `boardView.ts`: `slotForElement(view: BoardViewDto, element: BoardElement): BoardMatchSlotDto | null`; `listForElement(view, element): {matches, results} | null` (schneidet auf `element.limit` zu); `gridForLayout(layout): {columns: number; rows: number}`; `elementScale(element, slotOrList, columns): number` (autoFit=false → 1, sonst densityScale-Verdrahtung wie `boardScale`, nur je Kachel)
  - `useBoardViewData(eventId, boardId)` — `usePolledEndpoint(getBoardView, refreshIntervalSeconds, [eventId, boardId])` analog `useAthleteBoardData`
  - `BoardRenderer({view, now})` — CSS-Grid nach Layout, eine `BoardTileView` je Kachel
  - `BoardTileView({tile, view, now})` — rotiert per `setInterval` über `tile.elements` (Index-State + Fade), Intervall `tile.rotationIntervalSeconds`
  - `BoardElementView({element, view, now})` — Switch nach `element.type`

- [ ] **Step 1: Failing Tests für die pure Logik** (`boardView.test.ts`): slotForElement findet den Slot per Offset; listForElement schneidet auf das Element-Limit zu (Server liefert das Maximum je Modus); gridForLayout (ONE_COLUMN→1×1, TWO_COLUMNS→2×1, THREE_COLUMNS→3×1, SIX_TILES→3×2); elementScale: autoFit=false → 1, volle Kachel skaliert < 1 bei vielen Booten. Testdaten minimal wie in `boardLayout.test.ts`.

- [ ] **Step 2: Tests rot laufen lassen** (`cd frontend && npx vitest run src/components/event/board/boardView.test.ts`)

- [ ] **Step 3: `boardView.ts` implementieren** — dünne, pure Funktionen; `elementScale` verdrahtet `densityScale(maxBoats([...]), columns, rowTextLines(longestTeamLabel([...]), columns))` über eine zu `BoardCard` kompatible Hüllstruktur (`{kind, key, match, result}`) und respektiert `autoFit === false`.

- [ ] **Step 4: Tests grün laufen lassen.**

- [ ] **Step 5: Komponenten implementieren.** Leitplanken:
  - `BoardMatchSlotElement`: rendert per `AthleteBoardColumnCard` + `AthleteBoardMatchCard` (laufend/anstehend, `showCountdown={element.showCountdown !== false}`) bzw. `AthleteBoardResultCard` (Ergebnis) bzw. Leertext je Offset-Vorzeichen (`event.boards.element.emptyCurrent/emptyUpcoming/emptyPast`). Titel: bei Offset 0 „Im Rennen", +n „Als Nächstes"/„In n Läufen", −n „Ergebnis"/„Vor n Läufen" — Übersetzungsschlüssel aus Task 10. `showCrew === false` blendet Crew-Zeilen aus, `showTimes === false` Zeiten/Zwischenstände: beides über neue optionale Props an `AthleteBoardMatchCard`/`AthleteBoardResultCard` (`showCrew?: boolean`, `showTimes?: boolean`, Default true — minimal-invasive Ergänzung, bestehende Aufrufer unverändert). `contrastColors === false` → neue optionale Prop `plain?: boolean` an beiden Karten: Status-Farbflächen werden `background.paper`/`text.primary`.
  - `BoardTileView`: `const [index, setIndex] = useState(0)`; `useEffect` mit `setInterval(rotationIntervalSeconds * 1000)` nur bei `elements.length > 1`; Wechsel mit `<Fade key={index}>`; Index bei Elementzahl-Änderung modulo kappen.
  - `BoardClockElement`: Veranstaltungsname (optional) + `now.toLocaleTimeString` groß, Typografie wie Kopf des alten `AthleteBoardView`.
  - `BoardTextElement`: zentrierter Text, `fontSize: 'clamp(1rem, 3vw, 4rem)'`, Zeilenumbrüche aus der Konfiguration erhalten (`whiteSpace: 'pre-wrap'`).
  - `BoardMatchListElement`: kompakte Zeilenliste (Zeit | Wettkampf | Runde/Lauf; bei RESULTS zusätzlich Sieger-Kurzform): bewusst neu und schlicht statt Wiederverwendung der alten Vollbild-Views.
  - Skalierung: Kachel-Wrapper setzt `'--ab-scale': elementScale(...)`, damit `scaled()` aus `athleteBoard/common.ts` in den Karten weiter greift.

- [ ] **Step 6: `npx tsc --noEmit` + `npx vitest run`** — neue Dateien fehlerfrei (Altkomponenten-Fehler bleiben bis Task 9).

- [ ] **Step 7: Commit** (`git commit -m "Add board renderer components"`)

---

### Task 9: Seiten + Routen — öffentliche Board-Seite, Umleitung, Verwaltungsseite entrümpeln

**Files:**
- Create: `frontend/src/pages/event/BoardDisplayPage.tsx`
- Modify: `frontend/src/pages/event/AthleteBoardPage.tsx` (wird Umleitung)
- Modify: `frontend/src/pages/event/EventInfoPage.tsx` (wird Verwaltungsseite, Task 11 füllt sie)
- Modify: `frontend/src/routes.tsx`
- Delete: `frontend/src/components/event/info/InfoViewDisplay.tsx`, `ViewRotationControl.tsx`, `views/AthleteBoardView.tsx`; `views/UpcomingMatchesView.tsx`, `views/LatestMatchResultsView.tsx`, `views/RunningMatchesView.tsx`, `views/SinglePlaceColored.tsx` **nur falls** `grep -rn` keine Verwendung außerhalb `event/info` zeigt — sonst stehen lassen und im Abschlussbericht nennen.

**Interfaces:**
- Produces: Route `boardDisplayRoute` = `board/$eventId/$boardId` (Parent `rootRoute`, öffentlich); `athleteBoardRoute` (`board/$eventId`) bleibt und leitet um.

- [ ] **Step 1: `BoardDisplayPage`** — trägt das, was heute `AthleteBoardPage`+`AthleteBoardView` sind: `useBoardViewData`, `useServerClock(data?.serverTime)`, Zustände notFound/initialLoad/loadFailed wie im alten `AthleteBoardView` (Texte wiederverwenden), dann `BoardRenderer`. Dazu Vollbild: auto-versteckender Button (Muster `EventInfoPage`: `onMouseMove`-Timer, Taste `f`, `Escape`), Umsetzung als `Dialog fullScreen` wie heute. Stale-Overlay: kleine „Stand …"-Zeile unten rechts (`position: absolute`), `warning.main` bei stale — Logik 1:1 aus `AthleteBoardView` (STALE_AFTER_MISSED_INTERVALS = 2).

- [ ] **Step 2: `AthleteBoardPage` wird Umleitung** — lädt `getPublicBoards`; bei ≥1 Board `<Navigate to="/board/$eventId/$boardId" params={{eventId, boardId: boards[0].id}} replace />` (TanStack `Navigate` aus `@tanstack/react-router`); bei 0 Boards Hinweistext (`event.boards.none`); Lade-Spinner solange offen. Kommentar an die Datei: Bestands-URL montierter Bildschirme, deshalb bleibt die Route.

- [ ] **Step 3: `routes.tsx`** — `boardDisplayRoute` neben `athleteBoardRoute` anlegen und im `rootRoute.addChildren` registrieren.

- [ ] **Step 4: Alte Komponenten löschen** (siehe Files, mit grep-Vorbehalt), Importe bereinigen. `EventInfoPage` vorläufig auf Minimalgerüst setzen (Überschrift + leere Liste), damit tsc grün wird — Task 11 baut sie aus.

- [ ] **Step 5: `npx tsc --noEmit`** — komplett grün. `npx vitest run` grün.

- [ ] **Step 6: Commit** (`git commit -m "Add public board display page with redirect from legacy url"`)

---

### Task 10: i18n — Schlüssel für Boards (de/en/da)

**Files:**
- Modify: `frontend/src/i18n/de/translations.json`, `frontend/src/i18n/en/translations.json`, `frontend/src/i18n/da/translations.json`

- [ ] **Step 1: Neue Schlüssel unter `event.boards.*`** (Auszug, sinngemäß in allen drei Sprachen; `event.info.athleteBoard.*`-Schlüssel für Karten bleiben):
  - `title` („Boards"), `none`, `create`, `edit`, `delete`, `confirmDelete`, `open` („Anzeigen"), `copyUrl`, `name`
  - `layout.label`, `layout.oneColumn`, `layout.twoColumns`, `layout.threeColumns`, `layout.sixTiles`
  - `tile.title` („Kachel {{index}}"), `tile.rotationInterval`, `tile.addElement`, `element.remove`
  - `element.type.match` („Lauf"), `element.type.matchList` („Laufliste"), `element.type.clock` („Uhr"), `element.type.text` („Hinweistext")
  - `element.offset` („Position"), `element.offsetCurrent` („Aktueller Lauf"), `element.offsetBefore` („{{count}} davor"), `element.offsetAfter` („{{count}} danach")
  - `element.showCrew`, `element.showCountdown`, `element.showTimes`, `element.contrastColors`, `element.autoFit`
  - `element.listMode.upcoming/results/running`, `element.limit`
  - `element.text`, `element.showEventName`
  - `element.titleCurrent` („Im Rennen"), `element.titleNext` („Als Nächstes"), `element.titleUpcoming` („In {{count}} Läufen"), `element.titleLastResult` („Letztes Ergebnis"), `element.titlePast` („{{count}} Läufe zurück")
  - `element.emptyCurrent` („Kein Lauf im Rennen"), `element.emptyUpcoming` („Kein weiterer Lauf geplant"), `element.emptyPast` („Noch kein Ergebnis")
  - `refreshInterval`
  - Entfernen: `event.info.viewTypes.*`, `event.info.configuration*`, `event.info.viewCreated/Updated/Deleted`, `event.info.addView/editView`, `event.info.displayDuration`, `event.info.dataLimit`, `event.info.noViews*` — vorher je Schlüssel `grep -rn` auf Restverwendung.

- [ ] **Step 2: `npx tsc --noEmit` + kurzer grep auf tote Schlüsselverwendungen.**

- [ ] **Step 3: Commit** (`git commit -m "Add board translations"`)

---

### Task 11: Verwaltungsseite + Board-Editor

**Files:**
- Create: `frontend/src/components/event/board/BoardsPanel.tsx` (Liste)
- Create: `frontend/src/components/event/board/BoardEditor.tsx` (Dialog)
- Modify: `frontend/src/pages/event/EventInfoPage.tsx`
- Delete: `frontend/src/components/event/info/InfoViewConfiguration.tsx`, `frontend/src/components/event/info/ViewConfigurationForm.tsx`

**Interfaces:**
- Consumes: `getBoards`/`createBoard`/`updateBoard`/`deleteBoard`, `useFetch`, `useFeedback`, `useConfirmation`, `BaseDialog`.
- Produces: `BoardsPanel({eventId})`; `BoardEditor({board: BoardDto | null, onSubmit(request: BoardRequest), onCancel})`.

- [ ] **Step 1: `BoardsPanel`** — Karte je Board: Name, Layout-Kurzinfo, Aktionen: Anzeigen (Link auf `/board/$eventId/$boardId`, neuer Tab), URL kopieren (`navigator.clipboard.writeText`), Bearbeiten, Löschen (mit `confirmAction`). „Board anlegen"-Button → Editor mit `null`. Nach jeder Mutation Reload über `reloadKey`-State (Muster alte `InfoViewConfiguration`).

- [ ] **Step 2: `BoardEditor`** — kontrollierter State `const [name, setName] = useState(...)`, `const [config, setConfig] = useState<BoardConfig>(...)` (kein react-hook-form: die Struktur ist verschachtelt-dynamisch):
  - Layout-Auswahl: `ToggleButtonGroup` mit vier Optionen (Mini-Grid-Icons per CSS). Beim Wechsel Kacheln beschneiden/auffüllen: `tiles.slice(0, n)` + Auffüllen mit `{rotationIntervalSeconds: 10, elements: [{type: 'MATCH', offset: 0, showCrew: true, showCountdown: true, showTimes: true, contrastColors: true, autoFit: true}]}`.
  - Kachel-Bereich im selben Raster wie das Layout (`display: grid`): je Kachel eine Karte mit Rotations-Intervall (nur sichtbar ab 2 Elementen) und Element-Liste; je Element ein Kopf (Typ-Select + Entfernen + Hoch/Runter-Pfeile statt DnD — YAGNI) und darunter die typspezifischen Felder:
    - MATCH: Offset-Select −6…+6 (Label über `element.offsetBefore/offsetCurrent/offsetAfter`), fünf Checkboxen.
    - MATCH_LIST: Modus-Select, Limit-Slider 1–20.
    - CLOCK: Checkbox Veranstaltungsname.
    - TEXT: mehrzeiliges Textfeld.
  - `refreshIntervalSeconds`-Slider 10–60 auf Board-Ebene.
  - Speichern ruft `onSubmit({name, config})`; der Server validiert (Task 2), Fehler via `useFeedback().error`.

- [ ] **Step 3: `EventInfoPage`** = Seitengerüst + `BoardsPanel` (Auth hat die Route heute schon nicht — Endpoints verlangen Privilegien, Maske bleibt hinter Login-Navigation wie bisher).

- [ ] **Step 4: `npx tsc --noEmit` + `npx vitest run`** — grün.

- [ ] **Step 5: Commit** (`git commit -m "Add board management page and editor"`)

---

### Task 12: Verifikation im Browser + Gesamtlauf

- [ ] **Step 1: Backend-Gesamtlauf**: `cd backend && JAVA_HOME=… ./mvnw clean test -Ddatabase.url=jdbc:postgresql://localhost:7662/ready2race-build` → BUILD SUCCESS.
- [ ] **Step 2: Frontend-Gesamtlauf**: `cd frontend && npx tsc --noEmit && npx vitest run && npm run build` → grün.
- [ ] **Step 3: App starten** (launch.json-Konfiguration aus dem Projekt, Seed `seed-foerde.sql` laut Memory „Förde-Seed"; HTTP_PORT-Kollision zwischen Worktrees beachten) und im Browser-Pane prüfen:
  - `/event/{id}/info`: Default-Board aus der Migration erscheint; Editor öffnet, Layout-Wechsel füllt Kacheln auf.
  - Board anlegen mit 6 Kacheln inkl. Rotation (2 Elemente in einer Kachel) → öffentliche URL öffnen: Grid stimmt, Rotation wechselt, Uhr läuft, Vollbild per Taste f.
  - `/board/{eventId}` (alte URL) leitet auf das erste Board um.
  - Slots: bei laufendem Lauf aus dem Seed zeigt Offset 0 den Lauf, −1 das Ergebnis davor.
- [ ] **Step 4: Screenshot der Board-Anzeige an den Nutzer schicken.**
- [ ] **Step 5: Commit** letzter Korrekturen.

---

## Offene Melde-Pflichten an den Nutzer (Abschlussbericht)

- Migrationsnummer **V202608102000** (Regel für parallele Sessions).
- Alte Exporte (webDAV) tragen `infoViewConfigurations` — wird beim Import ignoriert, Boards aus Altexporten gehen nicht mit über.
- Handtest-Katalog: neue Fälle für Boards nötig (Editor, Rotation, Umleitung, Vollbild); bestehende Athleten-Board-Blöcke A–D aus dem Testkatalog treffen jetzt die Default-Board-Darstellung.
