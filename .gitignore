.idea
*.iml

deploy*.sh
/.cursor/
/CLAUDE.md

# Arbeitsverzeichnis der KI-Sitzungen: Hilfsskripte, Screenshots - und unter
# worktrees/ die parallelen Arbeitskopien selbst, die zusammen mehrere Gigabyte
# wiegen und jeweils ein eigenes Checkout dieses Repos sind. Nichts davon gehört
# in die Historie.
/.claude/

# Visual-Companion-Mockups aus dem Brainstorming
.superpowers/

# Abzüge der produktiven Datenbank. Sie enthalten echte Meldungen mit Klarnamen,
# Jahrgängen und E-Mail-Adressen und gehören weder in die Historie noch in einen
# Merge-Request. Die gepflegten Testseeds daneben (seed-dm-coastal-2026.sql und
# Geschwister) bleiben versioniert.
/docs/seeds/seed-prod-*.sql
